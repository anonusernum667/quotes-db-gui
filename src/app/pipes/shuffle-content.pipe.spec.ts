import { ShuffleContentPipe } from './shuffle-content.pipe';

describe('ShuffleContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ShuffleContentPipe();
    expect(pipe).toBeTruthy();
  });
});
